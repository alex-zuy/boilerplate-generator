package com.github.alex.zuy.boilerplate.config.validation;

import com.github.alex.zuy.boilerplate.config.ConfigException;
import com.github.alex.zuy.boilerplate.config.SupportClassesConfig;

public class SupportClassesConfigValidator implements ConfigValidator<SupportClassesConfig> {

    @Override
    public void validate(SupportClassesConfig config) throws ConfigException {
        // no need for validation
    }
}
